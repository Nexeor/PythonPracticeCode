pyg = 'ay'

# Get the raw input for our code, then make it into variable "original"
original = raw_input('Enter a word:')

# If statement checking to see if the word has characters and if all characters are letters. If not, print "empty"
if len(original) > 0 and original.isalpha():
    # Make all letters lowercase
    word = original.lower()
    # Get the first letter as a variable
    first = word[0]
    # Coctanate the word, the first letter, and "ay" under a new variable
    new_word = word + first + pyg
    # Remove the first letter of the word
    new_word = new_word[1:len(new_word)]
    # Print the word!
    print new_word
else:
    print 'empty'

